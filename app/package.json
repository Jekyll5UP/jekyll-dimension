{
  "name": "jekyll-dimension",
  "version": "11.11.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "app:dev":"bundle install && bundle update && bundle exec jekyll serve --port 4050 --incremental --verbose --trace",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jeffrey Desir",
  "license": "MIT"
}
